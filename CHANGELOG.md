# 更新日志

本文件记录所有值得注意的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 计划中
- `docwarden init` 支持 monorepo（一次在多个子包内安装）
- 提供 Docker 一键起本地界面的镜像

---

## [1.2.0] — 2026-09-15

第一个对外发布的版本。此前只在单个仓库里自用，这一版把「实施包」变成了可安装的工具。

### 新增
- **两种接入方式**，按你的平台和网络环境挑：
  - **引用模式**（`init --no-vendor`）：仓库里只放配置和 workflow，CI 里直接
    `uses: <org>/docwarden@v1` 调工具。新增 `action.yml`；工具升级只改版本号，
    不用动使用者的仓库。
  - **vendor 模式**（默认）：脚本拷进仓库，CI 不需要联网取工具，**内网也能跑**。
    GitLab 走这条。
- **`docs-update.mjs --root <路径>`**：脚本与目标仓库可以不在同一个地方。
  这是引用模式的前提 —— 脚本跑在 Action 自己的目录里，仓库在工作区，必须显式指过去。
  加了回归测试钉死「文档写进目标仓库，而不是工具自己的目录」。
- **提示词可局部覆盖**：`<仓库>/scripts/prompts/` 里有同名文件就优先用它，
  没有就用工具自带的。这样引用模式下想调提示词也不必整包 vendor。
- **`USAGE.md`（使用说明）**：从零到跑通的逐步操作手册 —— 装 → **配模型** → 复核模块划分
  → 生成验收 → 接 CI → 日常使用 → 排错表 → 命令速查。README 讲「为什么」，它讲「怎么做」。
- **模型配置刻意留空，不预填任何厂商**。`.knowledge.mjs` 里的 `llm.baseUrl` / `llm.model`
  从空串起。理由：这个脚本会把源码读出来发给模型，**填哪个地址就等于把源码交给谁** ——
  那是使用者的决定，不该由工具代做。没配就**在开工之前直接失败**，报错里给出两种配法
  （改配置文件 / 设环境变量）并指明「只想零成本体检可以只跑 `--mode audit`」；
  而不是先跑成功、把代码悄悄发到一个默认地址去。`doctor` 也会报告模型接口的状态。
- **`docwarden init`**：自动探测项目结构，推断模块划分，把脚本、提示词、CI 配置
  一次装进目标仓库。装完即可跑，不需要手工对照文件清单拷贝。
- **`docwarden doctor`**：体检安装是否完整，逐项给出「缺什么、怎么补」。
  会认出当前是哪种接入方式；引用模式下会拦下「workflow 里引用的还是占位符仓库地址」
  这个写错了会静默失效的问题。
- **`docwarden update`**：跑一次 CI 用的那份脚本。你仓库里有脚本就用你的那份，
  没有（引用模式）就用工具自带的，自动把 `--root` 指到当前目录。
- **`docwarden serve` / `docwarden mcp`**：本地界面与 MCP 服务收进同一个入口
  （可选辅助，跟 CI 上是同一套逻辑）。
- CI 双平台：GitHub Actions 与 GitLab CI，四个触发契机（PR 预览 / 主干更新 /
  版本章节 / 每日巡检）。
- MCP 服务：把知识库连同**新鲜度状态**一起交给 AI 编程助手。

### 变更
- **工具更名为 `docwarden`（中文名「项目文档守护」）**。此前叫 `docs-studio`，而那个名字
  连同「文档工坊」的叫法都把重心指向「本地工作台」——和它真正的运行位置（CI 流水线）不符，
  容易让人以为它是个手动跑的本地工具。改名后包名、命令、环境变量前缀
  （`DOCS_STUDIO_ROOT` → `DOCWARDEN_ROOT`）与本地存储键一并统一。
- **指纹与状态统一到一份实现**（`scripts/lib/freshness.mjs`）。
  此前 CI 用 `sha256 + 裸拼接`、本地界面用 `sha1 + 长度前缀`，两边互相认不出对方的产出，
  会把「源码没变」误判成「源码变了」并全量重新生成 —— 纯烧额度。现在共用同一份，
  并且**认得两种历史算法的旧指纹**：换算法时只就地升级文档头部，不重新生成。
- **文档状态移入 front-matter**。原先用独立的 `status.json` 记录「谁确认过」，
  但任何模块更新都会改动同一个文件，多分支合并必然冲突，而冲突内容是一串机器哈希，
  人工无法判断该保留哪一边。现在写进每份文档自己的头部，冲突面降到单个模块文件。
- 文件列表的哈希改为**长度前缀加框**（`路径 NUL 字节数 NUL 内容 NUL`），
  避免「A 的结尾 + B 的开头」拼出与另一组文件相同的字节流。

### 修复
- 导出时正文指纹少算了一个换行，导致「已验证」的文档在再次导出后无故退回「候选」。
- 判断确认是否仍然有效时方向搞反了：只证明了「旧确认当时有效」，
  没有证明「新写进去的内容与旧确认锁定的内容一致」。现在比对的是**即将写入的正文**。
- **模块推断的打分曲线两处退化**（`src/detect.mjs`），两处都是「不报错、但结果明显不对」：
  - 打分只按文件数给，导致「12 个文件一锅端」与「切成 3 个各 4 文件的模块」分数相同，
    唯一能裁决的层级惩罚又偏向更浅 —— 于是一个 `src/modules/{order,cart,auth}` 各 4 个
    文件的项目，被压成 `src/` 一个大模块。现在每个「够格成模块」的组额外记一笔，
    让细分在不让任何组掉出可用区间的前提下胜出。
  - 同分时偏向更浅的那一层，会把前缀一路放宽到 `src/`（40 个文件挤在 `src/modules/`
    时实测如此）。`src/` 会吞掉整个源码树，之后新增任何目录都不再落空、巡检永不报警 ——
    而「新目录漏配」正是最隐蔽的一类故障。现在同分时取**更深、更具体**的那一层。
- **功能清单里出现多余的冒号**（`下单 :: : 创建订单`）。模型见过我们自己写进索引的
  ` :: `，照着抄很常见，而解析只认「第一个冒号」，于是把说明切成了 `: 创建订单`。
  而「按功能提问」正是靠这张表检索的 —— 名字带脏字符没人会去修，只会让检索一点点失效。
  现在「一条功能点怎么拆」收敛成 `splitFeature` **一处实现**（`lib/freshness.mjs`），
  全角冒号、半角冒号、` :: ` 三种都认，读写共用同一套规则。
- **文档头部没有记录生成所用的模型**。README 承诺「元数据记录 `model` / `prompt_version`」
  用于区分换模型后的质量断层，但脚本从来没往头部写这两个字段。现在都写：
  `model` 取配置里的模型名，`prompt_version` 取**提示词模板的内容指纹**（改一个字就换一版，
  不需要有人记得手动加版本号）。**只记模型名，不记接口地址** —— 地址可能是内网地址或
  某家的私有网关，写进随代码提交的文档里等于把它公开。

[未发布]: https://github.com/xiaoanbucai/docwarden/compare/v1.2.0...HEAD
[1.2.0]: https://github.com/xiaoanbucai/docwarden/releases/tag/v1.2.0
