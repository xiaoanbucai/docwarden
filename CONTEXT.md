# docwarden · 项目文档守护

给团队一份敢信的模块文档：跑在 CI 上生成/刷新模块文档，并给每篇盖上不会说谎的状态章。
本文件是术语表，只定义"是什么"，不写实现。

## Language

### 文档状态（三态，写在每篇文档自己的 front-matter 里）

**候选（candidate）**:
模型生成、还没有任何人工确认的文档。引用其结论前须自行核对源码。
_Avoid_: 草稿、待定

**已验证（validated）**:
有人逐字确认过，且确认之后源码没有再变。文档中唯一"敢信"的状态。
_Avoid_: 已审核、已完成

**已失效（stale）**:
生成或确认之后源码发生了变更。只代表"源码已变、待复核"，**不代表内容写错了**。
_Avoid_: 已作废、错误

### 指纹

**源码指纹（source fingerprint）**:
一个模块全部源码文件集的 sha256 短摘要（16 位），记录在文档 front-matter 里，用来判断"解释的代码变了没有"。
_Avoid_: code_hash（旧字段名，已兼容读取）

**正文指纹（body fingerprint）**:
文档正文的指纹。已验证时另存一份 `validated_body_fingerprint`，防止"确认之后有人偷改正文"被静默放过。

### 行为语义

**回滚复活**:
已失效的文档，若源码被回滚到指纹重新匹配的状态，会自动恢复为已验证。
这是有意设计（文档与代码一致即为真），不是 bug；失效期间若正文被改过，正文指纹对不上，不会静默复活。

**确认**:
把一篇文档从候选（或失效后重新生成）变为已验证的人工动作，记录确认人与时间。
_Avoid_: 通过、批准

**确认人（validated_by）**:
执行确认动作的人的署名，自动取 git 提交署名（user.name），取不到时显式传入。
它是一份声明，不是鉴权——名字谁都能填，可信度来自"敢用真名署名"。
_Avoid_: 审核人（听着像有人审核过资质，其实只是署名）

**强制确认（--force）**:
对已失效文档的直接确认，等于人工背书"这次源码变更不影响本文档的结论"。
不走重新生成的正常路径，故要求显式付出这个摩擦成本。

**定位**:
docwarden 是通用工具：机制只依赖"目录 → 模块 → 文档 → 指纹"，不绑定任何编程语言。
示例与默认值可能带 JS/TS 味，那只是示例，不是承诺。

### 结构

**模块（module）**:
一组源码文件 + 对应的一篇文档，是生成与失效判定的最小单位。
_Avoid_: 包、目录（目录只是模块的常见来源，不是定义）

**知识库（knowledge base）**:
全部模块文档 + 索引（`docs/current/`）与版本章节（`docs/history/`）的整体。
_Avoid_: 文档库、status.json（项目刻意不设集中状态文件，见 ADR-0001）
